# Fringe

Static GitHub Pages site for `fringe.github.io`.

## Publish

1. Create a GitHub repository named `fringe.github.io`.
2. Add these files to the repository root.
3. Push to the default branch.

Because this is a plain static site with an `index.html` entry point, GitHub Pages can serve it directly with no build step.
